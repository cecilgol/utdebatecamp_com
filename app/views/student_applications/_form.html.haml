= form_for @student_application do |f|
  - if @student_application.errors.any?
    #error_explanation
      %h2= "#{pluralize(@student_application.errors.count, "error")} prohibited this student_application from being saved:"
      %ul
        - @student_application.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
