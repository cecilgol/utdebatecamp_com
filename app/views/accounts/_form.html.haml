= form_for @account do |f|
  - if @account.errors.any?
    #error_explanation
      %h2= "#{pluralize(@account.errors.count, "error")} prohibited this account from being saved:"
      %ul
        - @account.errors.full_messages.each do |msg|
          %li= msg

  %h3 Here is where you sign up for the UTNIF!
  %p We're so happy you're thinking about joining us for the best forensics experience available during the summer!
  %p Lets get some quick information about you!
  %p 
    I am signing up for 
    =f.select :eid, options_from_collection_for_select(@camps, "id", "name", "1"),{}, {class: 'c-select',id: 'csdd'}

  .actions
    = f.submit 'Save', {'class'=>'btn btn-primary'}
