%h1.block-title.m-b-md
  %span.text-ribbon.text-ribbon-info 
    %span 
      Listing Frequently Asked Questions
%p.lead
  %span.text-ribbon.text-ribbon-info
    %span 
      there are currently
      = @faqs.count
      faqs
      = link_to 'New Faq', new_admin_faq_path, {class:'btn btn-info btn-square', style:'float:right;'}


- @faqs.each do |faq|
  .row
    .col-xs-2
      =faq.question
    .col-xs-5
      =faq.answer
    .col-xs-1
      = link_to 'Edit', edit_admin_faq_path(faq), {class:'bg-info text-info'}
      = link_to 'Destroy', admin_faq_path(faq), {class:'bg-info text-info',:method => :delete, :data => { :confirm => 'Are you sure?' }}


